fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# LLM providers (one required — all three installed so you can switch)
groq>=0.9.0               # Groq — free Llama 3.3 70B (PRIMARY)
google-genai>=0.8.0       # Google Gemini AI Studio (fallback)
anthropic==0.36.1         # Claude fallback
openai==1.51.0            # GPT-4o fallback

# Supabase persistence (optional — non-fatal if missing)
supabase==2.9.1
httpx==0.27.2

# Netlist generation
skidl==1.2.3

# Utilities
python-multipart==0.0.12
jsonschema==4.23.0
